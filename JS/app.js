var options = {
  animate: true,
  patternWidth: 100,
  patternHeight: 100,
  grainOpacity: 0.13,
  grainDensity: 1,
  grainWidth: 2.19,
  grainHeight: 1,
};
grained("#tv-grain", options);
grained("#tv-grain-desktop", options);
